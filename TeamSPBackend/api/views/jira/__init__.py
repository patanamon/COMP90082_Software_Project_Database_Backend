# enable the code below to make jira.auto_get_ticket_count_team_timestamped() run on server startup, the process of fetching all data could be slow

# default_app_config = 'TeamSPBackend.api.views.jira.apps.AutoConfig'